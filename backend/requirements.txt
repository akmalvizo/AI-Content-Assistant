# AI Content Assistant — Backend Dependencies
# Pin exact versions for reproducible builds.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Configuration management
pydantic-settings==2.3.1

# Environment variable loading
python-dotenv==1.0.1

# Groq AI SDK — official Python client (requires httpx >= 0.28)
groq==1.5.0
