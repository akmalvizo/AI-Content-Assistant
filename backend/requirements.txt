# AI Content Assistant — Backend Dependencies
# Pin exact versions for reproducible builds.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Configuration management
pydantic-settings==2.3.1

# HTTP client (for outbound LLM API calls — Phase 2)
httpx==0.27.0

# Environment variable loading
python-dotenv==1.0.1
